name: Bot IA Manhã (08h BRT)
on:
  schedule:
    - cron: '0 11 * * *'
  workflow_dispatch:
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Disparar função
        run: |
          curl -f -X POST \
            -H "x-trigger-secret: ${{ secrets.TRIGGER_SECRET }}" \
            --max-time 300 \
            "https://curious-scone-6f4801.netlify.app/.netlify/functions/bot-manha"
